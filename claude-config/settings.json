{
  "permissions": {
    "allow": [
      "Bash(sudo -u postgres psql -c \"\\\\l\")",
      "Bash(sudo -u postgres psql -d timesheet_v3 -c \"SELECT version, description, success FROM flyway_schema_history;\")",
      "Bash(curl -sf http://localhost:8082/actuator/health)"
    ]
  },
  "hooks": {
    "Notification": [],
    "PermissionRequest": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "notify-send 'Claude Code' 'Requesting permission to execute' --urgency=critical --icon=dialog-question --expire-time=5000"
          }
        ]
      }
    ]
  }
}
